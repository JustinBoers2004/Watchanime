function toggleMenu() {
    const menu = document.querySelector('.menu');
    menu.classList.toggle('active');
  }
  
  const menuToggle = document.querySelector('.menu-toggle');
  menuToggle.style.display = 'flex'; // Display the menu toggle button for smaller screens
  
